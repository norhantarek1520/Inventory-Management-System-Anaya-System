// Defined roles enum matching your system specification
export enum Role {
  SUPER_ADMIN = 'super_admin',
  ADMIN = 'admin',
  PROCUREMENT_MANAGER = 'procurement_manager',
  INVENTORY_STAFF = 'inventory_staff',
  SYSTEM_SERVICE = 'system_service',
}
